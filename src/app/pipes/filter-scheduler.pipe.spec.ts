import { FilterSchedulerPipe } from './filter-scheduler.pipe';

describe('FilterSchedulerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSchedulerPipe();
    expect(pipe).toBeTruthy();
  });
});
