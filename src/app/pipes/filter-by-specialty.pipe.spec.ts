import { FilterBySpecialtyPipe } from './filter-by-specialty.pipe';

describe('FilterBySpecialtyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBySpecialtyPipe();
    expect(pipe).toBeTruthy();
  });
});
